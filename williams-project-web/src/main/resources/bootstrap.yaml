server:
  port: 8080
  servlet:
    context-path: /williams-project
spring:
  application:
    name: williams-project

logging:
  pattern:
    level: "%-5p [${spring.application.name},%mdc{trace_id},%mdc{span_id},]"